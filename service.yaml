ports:
  - port: 80
    targetPort: 5000

apiVersion: v1
kind: Service
metadata:
  name: mywebapp
  labels:
    app: mywebapp
spec:
  selector:
    app: mywebapp
  ports:
    - port: 80
      targetPort: 5000
